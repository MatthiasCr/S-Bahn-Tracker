import { Polyline } from 'leaflet';
import { type Trip } from '../services/api'

function TripLine({ trip }: { trip: Trip }) {



    return (
        <>
            {/* <Polyline positions={trip.polyline} /> */}
        </>
    )
}

export default TripLine;
